language: csharp

os:
- linux

mono:  
  - 5.0.1.1
  
branches:
  only:
    - develop

branches:
  except:
  - master
  
script:
  - ./build.sh --verbosity Diagnostic

cache:
  directories:
    - $HOME/.local/share/NuGet/Cache
    - $HOME/.nuget/packages
    - tools
    